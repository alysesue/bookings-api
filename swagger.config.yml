swagger:
  outputDirectory: ./dist
  entryFile:
    - ./src/**/*.controller.ts
  outputFormat: OpenApi_3
  name: GovTech Booking API
  description: Booking API
  license: PALO-IT
  produces: [application/json]
  version: 0.0.1
  securityDefinitions:
    default:
      type: apiKey
      in: header
      name: Authorization
    secondAuthenticator:
      type: apiKey
      in: header
      name: Authorization
